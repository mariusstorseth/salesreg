<%= simple_form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
    <%= f.input :first_name %>
  </div>
  <br />
  <div class="form-inputs">
    <%= f.input :last_name %>
  </div>
  <br />
  <div class="form-inputs">
    <%= f.input :email %>
  </div>
  <br />
  <div class="form-inputs">
    <%= f.input :username %>
  </div>
  <br />
  <div class="form-inputs">
    <%= f.input :office_id, collection: Office.all, label_method: :name %>
  </div>
  <br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
